const EXCHANGES = {
  READY_EXCHANGE: 'ready.exchange',
};

const QUEUES = {
  READY_QUEUE: 'ready.queue',
};

const ROUTING_KEYS = {
  FOO_READY_KEY: 'foo',
  BAR_READY_KEY: 'bar',
  BAZ_READY_KEY: 'baz',
};

export { EXCHANGES, QUEUES, ROUTING_KEYS };
